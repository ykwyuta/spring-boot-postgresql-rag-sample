<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.transportrag.project.ProjectMapper">
  <select id="findBySubject" resultType="com.example.transportrag.project.ProjectInfo">
    SELECT p.code, p.name, p.description, m.project_role AS role
    FROM public.user_project_memberships m
    JOIN public.projects p ON p.code = m.project_code
    WHERE m.subject = #{subject}
      AND p.active
    ORDER BY p.code
  </select>
</mapper>
